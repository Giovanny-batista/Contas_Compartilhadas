name: contascompartilhadas
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  firebase_core: ^3.3.0
  firebase_auth: ^5.1.4
  image_picker: ^0.8.4+4
  cloud_firestore: ^5.2.1
  uuid: ^4.4.2
  fl_chart: ^0.69.0 # Ou a versão mais recente disponível
  intl: ^0.18.1
  url_launcher: ^6.0.9
  # qr_flutter: ^4.1.0
  # qr_flutter: ^6.1.0
  pie_chart: 5.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - asset/contas.jpeg
    - asset/contas1.png
